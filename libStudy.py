# 라이브러리

import math # 파이썬 내장 math "모듈" 가져옴 ㅇㅇ
from math import sqrt # math 모듈 중에 sqrt "함수"만 가져옴
import numpy as np # numpy라는 "모듈" 을 'np' 라는 이름으로 사용 하겠다.
# loot 값 가져옴
print(math.sqrt(16))
print(sqrt(81))
print(np.array([1,2,3]))